import { Placeholder } from '../components/Placeholder';

export function LauncherPage() {
  return (
    <Placeholder
      title="라운드 런처"
      subtitle="page2 — refine 폼 + 인플레이스 진행 콘솔 (다음 단계)"
    />
  );
}
